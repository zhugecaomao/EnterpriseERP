{TODO :
(2003-03-29 15:22:17)   老姜
把初步使用情况发给你：
1、做成可以指定按那个字段进行查询或过滤，现在只能是第一列的字段
2、做成能保上一次的查询结果，而不是每次都要从新查询
3、做成获得输入焦点时就自动弹出下拉窗口，而不是录入时才弹出下拉窗口。
4、是否可以设置成不区分大小写字母
5、DBGRID不能自动计算合计，刚打开一个单据是可以的，上移下移单据就不会自动的。
其中1,2,3，5是重点，能帮助修改吗？
6、合计栏一行，经常性不能显示。MDI子窗口中最大化就不会显示，窗口调整过后才可以显示，最大化后又不见了。
7、能不能设置行的高度，我想实现字体不变，但行高度增大，这样看起  来会舒服些，也是为了满足用户的需求。}
